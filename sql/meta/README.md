# sql/meta/

ETL metadata and supporting SQL artifacts (not staging or curated logic).

- Run logs, execution metadata, or pipeline state.
- Schema definitions, table comments, or data dictionary source.
- Any SQL used for validation or one-off metadata queries.

No raw or curated data; code and config only.
