# Ingest + transform dependencies. Install from repo root: pip install -r python/requirements.txt
# Optional: use a venv: python -m venv .venv && .venv\Scripts\activate (Windows) or source .venv/bin/activate (Unix)

boto3>=1.26.0
lxml>=4.9.0
numpy>=1.20.0
openpyxl>=3.0.0
pandas>=1.3.0
polars[rt64]>=1.0.0
duckdb>=1.5.0
pyarrow>=10.0.0
requests>=2.25.0
tqdm>=4.62.0
